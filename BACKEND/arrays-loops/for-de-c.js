const nomeDeEstudantes = ['jose', 'joao', 'maria', 'joana', 'andre'];

for (let i = 0; i < nomeDeEstudantes.length; i++) {
    console.log(nomeDeEstudantes[i]);
}

// let indice = 0;

// while(indice < 5) { // enquanto
//   console.log(nomeDeEstudantes[indice]);
//    indice++;
// }